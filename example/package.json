{
  "name": "fastify-light-ddd-example",
  "version": "1.0.0",
  "description": "Example usage for fastify-light-ddd plugin",
  "main": "index.js",
  "scripts": {
    "start": "node  index.js",
    "lint:js:fix": "eslint . --fix",
    "lint:fix": "yarn lint:js:fix && yarn prettier:fix",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "assert": "^2.0.0",
    "dotenv": "^16.0.3",
    "fastify": "^3.20.2",
    "fastify-light-ddd": "^1.0.1"
  },
  "devDependencies": {
    "eslint": "^7.32.0"
  }
}
